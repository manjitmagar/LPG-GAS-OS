<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gas App</title>
    <link rel="icon"  type="image/png" href="/public/img/gas.png" >
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" href="/css/app.css">
 
  </head>

  <body>
    <nav class="container mx-auto flex items-center justify-between py-4">
      <div>
        <a href="/" class="inline-block px-4 py-0 rounded-full">
          <img class="rounded-full" src="/public/img/gas.png" alt="logo" height="30px" width="70px">
        </a>
      </div>
      <div>
        <ul class="flex items-center">
          <li class="ml-6"><a href="/">Our Products</a></li>
          <% if(user && user.role !== 'admin') { %>
            <li class="ml-6"><a href="/customer/orders">My Orders</a></li>
          <% } %>
          <% if(user && user.role == 'admin') { %>
            <li class="ml-6"><a href="/admin/products">Products</a></li>
            <li class="ml-6"><a href="/admin/product/add">Add Product</a></li>
            <li class="ml-6"><a href="/admin/orders">Orders</a></li>
            <li class="ml-6"><a href="/admin/users">Users</a></li>

          <% } %>
          <% if(user) { %>
            <li class="ml-6">
              <form id="logout" action="/logout" method="POST">        
                <a onClick="document.getElementById('logout').submit()" href="#">Logout</a>
              </form>
            </li>
          <% } else { %>
            <li class="ml-6"><a href="/register">Register</a></li>
            <li class="ml-6"><a href="/login">Login</a></li>
          <% } %>
          <li class="ml-6">
            <a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
              <span id="cartCounter" class="text-white font-bold pr-2"><%= session.cart ?  session.cart.totalQty : '' %></span>
              <img src="/public/img/pngegg (1).png" alt="cart" height="20px" width="30px">
            </a>
          </li>
        </ul>
      </div>
    </nav>
    <% if(user) { %>

      
      <!-- Additional content for logged-in users -->
    <% } %>

    <%- body %>

    <footer></footer>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
  </body>
</html>
